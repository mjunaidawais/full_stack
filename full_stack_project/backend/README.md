## Node/Express backend (modular monolith)

### Run locally

1) Create `backend/.env` (copy from `backend/.env.example`)
2) Install deps:

```bash
cd backend
npm install
```

3) Start:

```bash
npm run dev
```

- API: `http://localhost:5000/api`
- Health: `http://localhost:5000/health`
